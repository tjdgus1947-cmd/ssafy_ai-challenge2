# GPU 환경에 맞는 torch 를 먼저 설치한 뒤 나머지를 설치하는 것을 권장한다.
#   예) pip install torch --index-url https://download.pytorch.org/whl/cu128
torch
torchvision
transformers>=4.57.0   # Qwen3-VL 지원
accelerate>=0.34.2
peft>=0.13.2
bitsandbytes>=0.43.3
datasets
pillow
pandas
numpy
tqdm

# OCR 보조 채널 (선택)
easyocr
